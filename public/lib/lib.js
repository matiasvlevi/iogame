/**
 * p5js Canvas Setup
 */
let wnx = window.innerWidth;
let wny = window.innerHeight;

function setup() {
  createCanvas(wnx, wny);
}

/**
 * p5js Canvas Setup
 */
function draw() {
  background(51);
}